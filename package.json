{
  "name": "pdxhc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "og:image": "node scripts/generate-og-card.mjs",
    "verify:layout": "node scripts/verify-layout.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "cf:preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=pdxhc"
  },
  "dependencies": {
    "@lucide/vue": "latest",
    "vue": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "latest",
    "playwright": "^1.61.1",
    "vite": "latest",
    "wrangler": "latest"
  }
}
